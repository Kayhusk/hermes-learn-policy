# Hermes Learn Policy installed

If the plugin is disabled, enable it:

```bash
hermes plugins enable hermes-learn-policy
```

Restart Hermes or the active gateway, then verify the installation:

```bash
hermes plugins list
hermes plugins doctor hermes-learn-policy --ci
```

See [README.md](README.md) for profile setup, requirements, full verification, removal, and limits.
